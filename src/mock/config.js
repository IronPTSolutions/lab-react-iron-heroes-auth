// URL base de la API simulada. Las peticiones a este dominio son interceptadas
// por MSW y respondidas por los controllers (auth-controller.js / heroes-controller.js).
export const baseMockDomain = 'https://api.ironheroes.mock.org';
